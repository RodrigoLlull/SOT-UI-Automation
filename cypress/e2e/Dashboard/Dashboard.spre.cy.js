import generateRandomString from "../../support/Helpers/stringRandomHelpers"
import dashboardPage from "../../support/PageObject/dashboard.page"
import loginPage from "../../support/PageObject/login.page"

/* describe('Dashboard spec', () => {

    beforeEach(() => {
        const randomId = generateRandomString(4)
        cy.loginByApi(randomId)
        cy.visit("/dashboard")
    })

    it('Access to Test-Design', () => {
        dashboardPage.AccessToDesignPage();
        cy.url().should("eq", "https://d2yqnm7qbjnp0v.cloudfront.net/test-design?id=PROJECT1726858662783&name=GROUP+06")        
    })

    it('Access to Test-Runs', () => {
        dashboardPage.AccessToRunPage();
        cy.url().should("eq", "https://d2yqnm7qbjnp0v.cloudfront.net/test-runs?id=PROJECT1726858662783&name=GROUP+06")        
    })

}) */